[...document.querySelectorAll('form.user-form div.mb-3 label')].map(e => {e.className="form-label"});
[...document.querySelectorAll('form.user-form div.mb-3 input')].map(e => {e.className="form-control"});
[...document.querySelectorAll('form.user-form div.mb-3 select')].map(e => {e.className="form-select"});
[...document.querySelectorAll('form.user-form div.mb-3 ul.errorlist li')].map(e => {e.className="text-danger"})
